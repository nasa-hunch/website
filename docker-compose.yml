# Set the version of docker compose to use
version: '3.9'

# The containers that compose the project
services:
  db:
    image: postgres:13
    restart: always
    container_name: nasa-hunch-postgres
    environment:
      POSTGRES_USER: prisma
      POSTGRES_PASSWORD: prisma
      POSTGRES_DB: tests

  web:
    image: node:latest
    container_name: nasa-hunch-web
    environment:
      - OUTER_PORT_FRONTEND=3000
      - INNER_PORT_FRONTEND=5173
    ports:
      - '3000:5173'
    volumes:
      - ./:/app
      - /app/node_modules/
    working_dir: /app
    command: bash -c "yarn && npx dotenv-cli -e .env.test -- yarn db:deploy && yarn dev --host --port 5173"
    depends_on:
      - db